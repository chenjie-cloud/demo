# AGENTS.md

## Cursor Cloud specific instructions

This repository (`chenjie-cloud/demo`) is currently an empty scaffold containing only a blank `README` file. There is no application code, no dependencies, no build system, and no services to run.

**Available tooling in the Cloud VM:** Git 2.43, Node.js v22 (via nvm), Python 3.12, npm, pnpm, yarn.

When application code is added to this repository, update this section with:
- How to install dependencies
- How to run lint, tests, and the dev server
- Any non-obvious caveats for the development environment
